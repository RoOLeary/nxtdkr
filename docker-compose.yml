version: '3.9'
services:
  app:
    image: nxtdkr
    build:
      context: ./
      target: production
      dockerfile: Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
        - /app/.next
        - /app/public
    ports:
      - "3000:3000"